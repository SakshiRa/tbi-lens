model:
  name: UNet
  params:
    spatial_dims: 3
    in_channels: 1
    out_channels: 2
    channels: [16, 32, 64, 128, 256]
    strides: [2, 2, 2, 2]
    num_res_units: 2
  strict: true

transforms:
  orientation: RAS
  spacing: [1.0, 1.0, 1.0]
  spacing_mode: bilinear
  intensity:
    a_min: -1000
    a_max: 1000
    b_min: 0.0
    b_max: 1.0
    clip: true

inference:
  roi_size: [96, 96, 96]
  sw_batch_size: 4
  overlap: 0.25
  device: cpu

post:
  activation: softmax
  argmax: true

ground_truth: null
